(npm install -g @githubnext/copilot-cli)

# Or:

(pnpm add -g @githubnext/copilot-cli)

(copilot auth login)

(copilot help)             

(npm install --save-dev hardhat ethers @nomiclabs/hardhat-ethers

# (Optional: For contract verification)

(npm install --save-dev @nomiclabs/hardhat-etherscan)

# (Optional: For .env private key/secrets management)

(npm install dotenv)
