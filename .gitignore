# Environment files — NEVER commit secrets
.env
.env.local
.env.production
.env.staging
.env.*.local

# Hardhat build artifacts
artifacts/
cache/
typechain-types/

# Node modules
node_modules/

# Deployment outputs (contain addresses — review before committing)
deployment-config*.json
multisig-transaction.json
signature-morpho-config.json

# IP metadata registration outputs (generated by register-ip-metadata.cjs)
ip-metadata-registration.*.json

# ABI / bytecode proof and IPFS manifest (generated by export-abi-proof.cjs
# and the ipfs-to-storyscan workflow — uploaded as GitHub Actions artifacts)
abi-proof.json
abi-proof-pin.json
ipfs-document-manifest.json

# Build outputs
dist/

# Log files
*.log
compile.log

# OS files
.DS_Store
Thumbs.db
